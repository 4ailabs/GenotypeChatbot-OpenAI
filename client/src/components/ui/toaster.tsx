// Componente de tostadora simplificado solo para satisfacer las importaciones
// En una implementación real, esto usaría un componente más sofisticado basado en shadcn/ui

import React from "react";

export function Toaster() {
  // No hace nada en esta implementación mínima
  // Estamos usando alert() nativo a través del hook use-toast
  return null;
}